name: Build & Deploy

on: [push]

jobs:
  build:
    runs-on: ubuntu-lastest

    steps:
      - uses: actions/checkout@v2
      - name: Run script file
        run: |
          sh ./deploy.sh
        shell: bash
