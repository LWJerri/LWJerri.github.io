language: node_js
node_js:
  - lts/*
install:
  - yarn
script:
  - yarn build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: dist
  github_token: $TOKEN
  keep_history: true
  fqdn: lwjerri.js.org
  on:
    branch: master
